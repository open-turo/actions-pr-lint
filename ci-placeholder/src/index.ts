/**
 * Placeholder module so the test workflow has real coverage output to report.
 * This package is removed once a real action package exists in the repo.
 */
export function noop(): boolean {
  return true;
}
